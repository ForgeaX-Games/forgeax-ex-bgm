# Source provenance

> [!IMPORTANT]
> This repository was extracted from
> `ForgeaX-Games/forgeax-marketplace@6e7b0de:extensions/BGM & SFX` on 2026-08-24.
> That tree is the last `0.4.5` audio-generation surface before Marketplace
> commit `769997a` converged the directory into `extensions/bgm` and restored the
> older local-library product surface.

The extraction copied the source tree byte-for-byte before applying the independent
repository changes: package and manifest identity became `@forgeax-extension/bgm`,
the release version became `0.5.0`, repository metadata and CI were added, and the
public extension route became `/extensions/bgm/`.
