{
  "type": "object",
  "properties": {
    "slug": {
      "type": "string",
      "description": "目标游戏 slug"
    },
    "projectId": {
      "type": "string",
      "description": "音频项目 ID；默认与 slug 相同"
    },
    "runtimeEvidence": {
      "type": "object",
      "description": "一局真实游玩后 gameAudio.getProfilerSnapshot() 的 JSON。有它才按出声验收，没有则只做静态校验。"
    },
    "phase": {
      "type": "string",
      "enum": [
        "preparation",
        "integration",
        "runtime"
      ],
      "description": "Check prepared assets before gameplay hooks exist, static integration, or real playback evidence. Defaults to integration, or runtime when runtimeEvidence is supplied."
    }
  },
  "required": [
    "slug"
  ],
  "additionalProperties": false
}
